class Persona {
  String nombre = ''; // Inicialización directa de la variable
  int edad = 0; // Inicialización directa de la variable

  // Constructor por defecto proporcionado automáticamente
}

void main() {
  var persona = Persona(); // Llama al constructor por defecto
}
